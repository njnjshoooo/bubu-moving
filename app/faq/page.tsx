import {CustomerPage} from '@/components/customer-layout';import {questions} from '@/lib/customer-content';export const metadata={title:'搬家常見問題｜步步搬家'};
export default function Page(){const groups=[...new Set(questions.map(q=>q.group))];return <CustomerPage label="FREQUENTLY ASKED" title="把問題先說清楚，搬家更安心。" intro="收費、預約、打包準備與服務後的問題，先在這裡找到方向。個別物品與現場條件，歡迎一起討論。"><nav className="topic-links" aria-label="問題分類">{groups.map((g,i)=><a key={g} href={'#faq-'+i}>{g}</a>)}</nav>{groups.map((g,i)=><section className="faq-group" id={'faq-'+i} key={g}><h2>{g}</h2><div className="faq-list">{questions.filter(q=>q.group===g).map(q=><details key={q.q}><summary>{q.q}<span>＋</span></summary><p>{q.a}</p></details>)}</div></section>)}</CustomerPage>}
