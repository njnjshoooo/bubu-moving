 'use client';import {useState} from 'react';import {Select,SelectTrigger,SelectValue,SelectContent,SelectItem} from '@/components/ui/select';import {Input} from '@/components/ui/input';
export default function QuickStart(){const [service,setService]=useState('S01');const [area,setArea]=useState('');return <form className="quick-start" onSubmit={e=>{e.preventDefault();window.location.href='/inquiry?'+new URLSearchParams({service,from:area}).toString()}}><div><label htmlFor="quick-service">需要什麼協助？</label><Select value={service} onValueChange={value=>{if(['S01','S02','S04'].includes(value))setService(value)}}><SelectTrigger id="quick-service"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="S01">搬家搬運</SelectItem><SelectItem value="S02">打包整理</SelectItem><SelectItem value="S04">搬前／搬後清潔</SelectItem></SelectContent></Select></div><div><label htmlFor="quick-area">服務地區</label><Input id="quick-area" value={area} onChange={e=>setArea(e.target.value)} maxLength={150} placeholder="例如：新北市板橋區"/></div><button type="submit" className="primary">下一步，留下聯絡方式 ↗</button></form>}
