import {plans} from '@/lib/customer-content';import {VisualIcon} from '@/components/visual-explainer';
const combinations=[['truck'],['package','truck'],['truck','clean']] as const;const titles=['只需要搬運','整理＋搬家','搬家＋清潔'];
export function ServicePlans(){return <section className="plan-section"><div className="section-title"><div><span className="eyebrow">CHOOSE YOUR MOVE</span><h2>你需要哪一種協助？</h2></div><p>單項或搭配都可以，費用逐項確認。</p></div><div className="content-grid">{plans.map((p,i)=><article className="info-card plan-card" key={p.codes}><div className="combo-icons">{combinations[i].map((k,j)=><div key={k}>{j>0&&<span className="combo-plus">＋</span>}<VisualIcon kind={k}/></div>)}</div><h3>{titles[i]}</h3><p>{['已經打包好，請步步來搬。','一起分類裝箱，再搬進新家。','安排搬運，也把空間整理乾淨。'][i]}</p><details className="mini-details"><summary>查看服務範圍</summary><ul>{p.items.map(x=><li key={x}>{x}</li>)}</ul></details><a className="text-link" href={'/inquiry?services='+p.codes}>我需要這個 ↗</a></article>)}</div></section>}
